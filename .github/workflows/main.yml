name: Update GitHub B

on:
  push:
    branches:
      - main # Change this to the branch you want to monitor

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
    - name: Check Out Code
      uses: actions/checkout@v3

    - name: Update GitHub B
      run: |
        git config --global user.name "devops-team-poc"
        git config --global user.email "devops.consultancy@ongraph.com"
        git remote add githubA https://github.com/devops-team-poc/terraform-poc.git
        git push githubA main
      env:
        GH_B_TOKEN: ${{ secrets.GH_B_TOKEN }}
